// Replace with your deployed backend URL (e.g. https://your-app.onrender.com)
export const API_BASE_URL = 'https://REPLACE_ME.onrender.com';

// Cloudinary is used to host photos so photo_url is a real, publicly viewable link
// (the officials' web dashboard can't reach a file stored on a citizen's phone).
// Create a free account at https://cloudinary.com, then fill these in from your dashboard.
export const CLOUDINARY_CLOUD_NAME = 'REPLACE_ME';
export const CLOUDINARY_UPLOAD_PRESET = 'REPLACE_ME';
